// course.model.ts

export interface Course {
    id: number;
    name: string;
    description: string;
    createdDate: string;
    // Add any other properties if present in your API response
  }
  